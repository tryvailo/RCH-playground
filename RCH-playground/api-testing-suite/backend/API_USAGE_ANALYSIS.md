# Анализ использования CQC и Companies House API для матчинга

**Дата:** 2025-12-18  
**Тест:** Первая анкета (professional_questionnaire_1_dementia.json)

---

## 📊 Детальная раскладка скоринга для первого дома

### Дом: Meadow Rose Nursing Home

**Итоговый скор:** 51.0/100

### Веса категорий:
- Medical Safety: 45.0%
- Quality Care: 30.0%
- Location: 15.0%
- Financial: 8.0%
- Lifestyle: 2.0%

### Скоры по категориям:
- Medical Safety: 67.0/100 → 30.2 points (вес 45.0%)
- Quality Care: 50.0/100 → 15.0 points (вес 30.0%)
- Location: 5.0/100 → 0.8 points (вес 15.0%)
- Financial: 50.0/100 → 4.0 points (вес 8.0%)
- Lifestyle: 55.0/100 → 1.1 points (вес 2.0%)

---

## 🔌 Использование API данных

### ❌ CQC API: НЕ ИСПОЛЬЗУЕТСЯ (DB fallback)

**Причина:**
- CQC API возвращает `overall=Unknown` для всех домов
- Система автоматически использует данные из DB/CSV как fallback
- В логах: `✅ CQC data enriched for 1-1004508435: overall=Unknown`

**Где используется CQC в матчинге:**
1. **Medical & Safety → CQC Safe Rating (25 points)**
   - Проверяет `enriched_data['cqc_detailed']['safe_rating']`
   - Если нет → использует `home['cqc_rating_safe']` из DB/CSV
   - **Текущий статус:** Используется DB fallback, т.к. API не возвращает safe_rating

2. **Quality & Care → CQC Overall/Caring/Effective (100 points total)**
   - Проверяет `enriched_data['cqc_detailed']['overall_rating']`
   - Если нет → использует `home['cqc_rating_overall']` из DB/CSV
   - **Текущий статус:** Используется DB fallback, т.к. API возвращает "Unknown"

**Решение:**
- Проверить CQC API ключ (видна ошибка 401 в логах для Staff Quality)
- Убедиться, что API возвращает корректные данные
- Если API недоступен, система корректно использует DB fallback

---

### ❌ Companies House API: НЕ ИСПОЛЬЗУЕТСЯ (no data)

**Причина:**
- Companies House клиент не инициализируется
- В логах: `⚠️ Companies House enrichment failed: Failed to initialize Companies House client:`
- Нет данных для финансового анализа

**Где используется Companies House в матчинге:**
1. **Financial Stability → Altman Z-Score (30 points)**
   - Проверяет `enriched_data['companies_house_scoring']['altman_z_score']`
   - Если нет → проверяет `enriched_data['financial_data']['altman_z_score']`
   - Если нет → возвращает нейтральный скор (15 points)
   - **Текущий статус:** Нет данных → используется нейтральный скор

2. **Financial Stability → Revenue Trend (20 points)**
   - Проверяет `enriched_data['financial_data']['revenue_trend']`
   - Если нет → возвращает нейтральный скор (10 points)
   - **Текущий статус:** Нет данных → используется нейтральный скор

3. **Financial Stability → Red Flags (15 points)**
   - Проверяет `enriched_data['financial_data']['red_flags']`
   - Если нет → нет штрафов (15 points)
   - **Текущий статус:** Нет данных → нет штрафов

**Решение:**
- Проверить Companies House API ключ в конфигурации
- Убедиться, что сервис правильно инициализируется
- Проверить логи инициализации CompaniesHouseService

---

## 📋 Детальная раскладка Medical & Safety (67.0/100)

### Компоненты:
1. **Care Type Match: 30 points**
   - Проверка соответствия типа ухода (dementia/nursing/residential)
   - **Источник:** DB/CSV данные (`care_dementia`, `care_nursing`, etc.)

2. **CQC Safe Rating: 25 points**
   - **Источник данных:**
     - ✅ Приоритет 1: `enriched_data['cqc_detailed']['safe_rating']` (CQC API)
     - ⚠️ Приоритет 2: `home['cqc_rating_safe']` (DB/CSV fallback)
   - **Текущий статус:** Используется DB fallback
   - **Рейтинг:** Из DB/CSV
   - **Скор:** Зависит от рейтинга (Outstanding=25, Good=20, Requires improvement=10, Inadequate=0)

3. **Accessibility: 15 points**
   - Проверка wheelchair access
   - **Источник:** DB/CSV данные

4. **Medication Match: 15 points**
   - Проверка соответствия требованиям по медикаментам
   - **Источник:** Логика на основе анкеты + DB данные (care_nursing)

5. **Age Match: 10 points**
   - Проверка возрастных ограничений
   - **Источник:** DB/CSV данные (serves_older_people, serves_younger_adults)

6. **Special Needs: 5 points**
   - Проверка специальных потребностей (secure_garden для dementia)
   - **Источник:** DB/CSV данные

---

## 💰 Детальная раскладка Financial Stability (50.0/100)

### Компоненты:
1. **Budget Match: 35 points** ✅
   - **Источник:** Анкета (`q7_budget`) + DB/CSV цены (`fee_dementia_from`, etc.)
   - **Расчет:** Сравнение цены дома с бюджетом клиента
   - **Текущий статус:** ✅ Работает (использует данные из анкеты и DB)

2. **Altman Z-Score: 30 points** ❌
   - **Источник данных:**
     - ✅ Приоритет 1: `enriched_data['companies_house_scoring']['altman_z_score']` (Companies House API)
     - ✅ Приоритет 2: `enriched_data['financial_data']['altman_z_score']` (Companies House API)
     - ⚠️ Приоритет 3: Нет данных → нейтральный скор (15 points)
   - **Текущий статус:** ❌ Нет данных → используется нейтральный скор (15 points)
   - **Причина:** Companies House API не инициализируется

3. **Revenue Trend: 20 points** ❌
   - **Источник данных:**
     - ✅ Приоритет 1: `enriched_data['financial_data']['revenue_trend']` (Companies House API)
     - ⚠️ Приоритет 2: Нет данных → нейтральный скор (10 points)
   - **Текущий статус:** ❌ Нет данных → используется нейтральный скор (10 points)
   - **Причина:** Companies House API не инициализируется

4. **Red Flags: 15 points** ❌
   - **Источник данных:**
     - ✅ Приоритет 1: `enriched_data['financial_data']['red_flags']` (Companies House API)
     - ⚠️ Приоритет 2: Нет данных → нет штрафов (15 points)
   - **Текущий статус:** ❌ Нет данных → нет штрафов (15 points)
   - **Причина:** Companies House API не инициализируется

---

## 🔍 Выводы

### ✅ Что работает:
1. **Budget Match** - корректно использует данные из анкеты и DB
2. **Fallback механизм** - система корректно использует DB данные, если API недоступны
3. **Детальное логирование** - показывает источники данных для каждого компонента

### ❌ Что не работает:
1. **CQC API** - возвращает "Unknown" или ошибки 401
2. **Companies House API** - не инициализируется

### 📊 Влияние на итоговый скор:

**Текущий скор: 51.0/100**

**Если бы API работали:**
- CQC Safe Rating мог бы быть выше/ниже в зависимости от реального рейтинга
- Financial Stability мог бы быть выше/ниже в зависимости от:
  - Altman Z-Score (реальная финансовая стабильность)
  - Revenue Trend (растущий/стабильный/снижающийся)
  - Red Flags (наличие финансовых проблем)

**Потенциальное влияние:**
- Если CQC Safe = "Good" → +5 points в Medical & Safety
- Если Altman Z-Score = 2.7+ → +10-15 points в Financial
- Если Revenue Trend = "growing" → +5 points в Financial

**Итого:** API данные могут изменить итоговый скор на ±10-20 points

---

## 🛠️ Рекомендации

1. **Проверить CQC API ключ:**
   - Ошибка 401 указывает на проблему с ключом
   - Убедиться, что ключ активен и имеет правильные права доступа

2. **Проверить Companies House API:**
   - Проверить инициализацию CompaniesHouseService
   - Убедиться, что API ключ настроен в конфигурации
   - Проверить логи инициализации

3. **Улучшить обработку ошибок:**
   - Добавить более детальное логирование ошибок API
   - Показывать конкретные причины недоступности API

4. **Мониторинг:**
   - Отслеживать процент успешных вызовов API
   - Логировать случаи использования fallback данных

