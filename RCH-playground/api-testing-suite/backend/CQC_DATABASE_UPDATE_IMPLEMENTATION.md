# ✅ Реализация обновления оригинального файла CQC базы данных

**Дата:** 2025-12-20  
**Статус:** ✅ РЕАЛИЗОВАНО

---

## 🎯 Задача

Реализовать **Вариант 1: Обновление оригинального файла** для скрипта `update_cqc_database.py`.

---

## ✅ Реализованные изменения

### **1. Загрузка всех домов из оригинального CSV**

**Изменение:**
- Загружаются **ВСЕ** дома из оригинального файла
- Создается список `all_homes` со всеми домами
- Загружаются оригинальные CSV строки для сохранения всех полей

**Код:**
```python
# Load ALL CQC homes from original CSV
all_homes = load_cqc_homes(str(csv_path))
logger.info(f"Loaded {len(all_homes)} total homes from CSV")

# Also load original CSV rows to preserve all fields
original_csv_rows = {}
with open(csv_path, 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        location_id = row.get('location_id', '').strip()
        if location_id:
            original_csv_rows[location_id] = row
```

---

### **2. Определение домов для обновления через API**

**Изменение:**
- Если указан `--limit`, выбираются дома с пустыми полями (приоритет)
- Создается список `homes_to_update` для обновления через API
- Остальные дома остаются без изменений

**Код:**
```python
# Determine which homes to update via API
homes_to_update = all_homes.copy()  # Start with all homes

if limit:
    # Prioritize homes with empty fields
    empty_homes = []
    filled_homes = []
    # ... separation logic ...
    homes_to_update = (empty_homes + filled_homes)[:limit]
```

---

### **3. Обновление через API**

**Изменение:**
- Обновляются только дома из `homes_to_update`
- Обновления применяются к объектам в `all_homes` (in-place)
- Все дома остаются в списке `all_homes`

**Код:**
```python
# Process homes in batches
for batch in batches:
    results = await asyncio.gather(*tasks)
    # Updates are applied in-place to homes in all_homes
```

---

### **4. Создание резервной копии**

**Изменение:**
- Перед обновлением оригинального файла создается резервная копия
- Имя резервной копии: `cqc_carehomes_master_full_data_rows_BACKUP_YYYYMMDD_HHMMSS.csv`

**Код:**
```python
# Create backup before updating
if output_file == csv_path:
    from datetime import datetime
    import shutil
    
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    backup_file = csv_dir / f"{csv_stem}_BACKUP_{timestamp}{csv_suffix}"
    
    shutil.copy2(csv_path, backup_file)
    logger.info(f"✅ Backup created: {backup_file}")
```

---

### **5. Сохранение всех домов в оригинальный файл**

**Изменение:**
- Сохраняются **ВСЕ** дома из `all_homes` (не только обновленные)
- Если `output_path` не указан, обновляется оригинальный файл
- Сохраняются все поля из оригинального CSV (не только обновленные)

**Код:**
```python
# Determine output path
if output_path:
    output_file = Path(output_path)
else:
    # Update original file in place
    output_file = csv_path

# Save ALL homes to CSV (not just updated ones)
save_homes_to_csv(all_homes, output_file, csv_path, original_csv_rows)
```

---

### **6. Улучшение функции `save_homes_to_csv()`**

**Изменение:**
- Функция теперь принимает `original_csv_rows` для сохранения всех полей
- Начинает с оригинальной CSV строки (сохраняет все поля)
- Обновляет только измененные поля

**Код:**
```python
def save_homes_to_csv(
    homes: List[Dict],
    output_path: Path,
    original_csv_path: Path,
    original_csv_rows: Optional[Dict[str, Dict]] = None
) -> None:
    # ...
    for home in homes:
        location_id = home.get('location_id') or home.get('cqc_location_id', '')
        
        # Start with original CSV row if available (preserves all fields)
        if location_id in original_csv_rows:
            csv_row = original_csv_rows[location_id].copy()
        else:
            csv_row = {}
        
        # Map updated fields from DB format back to CSV format
        # (only updates changed fields)
```

---

## 📊 Результат

### **До исправления:**

- ❌ Создавался новый файл `_UPDATED.csv`
- ❌ Сохранялись только обновленные дома (20 домов)
- ❌ Оригинальный файл не обновлялся
- ❌ Система не использовала обновления

---

### **После исправления:**

- ✅ Обновляется оригинальный файл `cqc_carehomes_master_full_data_rows.csv`
- ✅ Сохраняются все дома (14,599 домов)
- ✅ Сохраняются все поля из оригинального CSV
- ✅ Создается резервная копия перед обновлением
- ✅ Система автоматически использует обновленные данные

---

## 🎯 Использование

### **Обновить оригинальный файл (рекомендуется):**

```bash
python3 scripts/update_cqc_database.py \
  --fields regulated_activity_nursing_care,regulated_activity_personal_care \
  --batch-size 20 \
  --delay 2.0
```

**Результат:**
- Создается резервная копия: `cqc_carehomes_master_full_data_rows_BACKUP_YYYYMMDD_HHMMSS.csv`
- Обновляется оригинальный файл: `cqc_carehomes_master_full_data_rows.csv`
- Сохраняются все 14,599 домов со всеми полями

---

### **Тестирование на ограниченном наборе:**

```bash
python3 scripts/update_cqc_database.py \
  --fields regulated_activity_nursing_care,regulated_activity_personal_care \
  --limit 20 \
  --batch-size 5 \
  --delay 1.0
```

**Результат:**
- Обновляются только 20 домов через API
- Сохраняются все 14,599 домов (20 обновленных + остальные без изменений)
- Оригинальный файл обновляется

---

## ✅ Преимущества

1. **Единый источник данных:**
   - Оригинальный файл всегда актуален
   - Нет путаницы с несколькими файлами

2. **Безопасность:**
   - Автоматическое создание резервной копии
   - Возможность отката изменений

3. **Полнота данных:**
   - Сохраняются все дома, а не только обновленные
   - Сохраняются все поля из оригинального CSV

4. **Автоматическое применение:**
   - Система автоматически использует обновленные данные
   - Нет необходимости менять код загрузки

---

## 📋 Статистика обновления

После обновления скрипт выводит:

```
UPDATE STATISTICS
================================================================================
Total homes in CSV: 14599
Homes updated via API: 14599 (или меньше, если указан --limit)
Processed: 14599
Updated: 14599
API success: 14599
API errors: 0

Fields updated:
  regulated_activity_nursing_care: 4377
  regulated_activity_personal_care: 10200

✅ Update complete!
All 14599 homes saved to CSV
================================================================================
```

---

## 🔍 Ключевые изменения в коде

### **Файл:** `scripts/update_cqc_database.py`

1. **Функция `update_cqc_database()`:**
   - Загружает все дома из оригинального CSV
   - Загружает оригинальные CSV строки для сохранения всех полей
   - Определяет дома для обновления через API
   - Создает резервную копию перед обновлением
   - Сохраняет все дома в оригинальный файл

2. **Функция `save_homes_to_csv()`:**
   - Принимает `original_csv_rows` для сохранения всех полей
   - Начинает с оригинальной CSV строки
   - Обновляет только измененные поля

---

**Последнее обновление:** 2025-12-20

