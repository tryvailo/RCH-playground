# Реализация сопоставления Staging и CQC данных

**Дата:** 2025-01-XX  
**Статус:** ✅ РЕАЛИЗОВАНО  
**Проблема:** Staging таблица не имеет поля `cqc_location_id` (все записи пустые)

---

## 📊 Проблема

Ранее предполагалось, что Staging и CQC таблицы связаны через `location_id`:
- CQC: `location_id` (формат: `1-10000302982`)
- Staging: `cqc_location_id` (ожидалось то же значение)

**Реальность:** Все записи в Staging имеют пустое поле `cqc_location_id`, поэтому прямое сопоставление по ID невозможно.

---

## ✅ Решение

Реализован многоуровневый алгоритм сопоставления по нескольким полям:

### Стратегии сопоставления (в порядке приоритета):

1. **Точное совпадение по Name + Postcode** (наиболее надежно)
   - Нормализация: приведение к нижнему регистру, удаление спецсимволов
   - Ключ: `normalize(name) + "|" + normalize(postcode)`

2. **Точное совпадение по Name + City**
   - Используется когда postcode отсутствует или не совпадает
   - Ключ: `normalize(name) + "|" + normalize(city)`

3. **Нечеткое совпадение по Name (similarity ≥ 0.8) + точный Postcode**
   - Для случаев, когда названия немного отличаются
   - Использует Jaccard similarity по словам

4. **Совпадение по Telephone**
   - Нормализация: удаление всех нецифровых символов
   - Используется как последний резерв

---

## 📁 Измененные файлы

### 1. `services/care_home_matcher.py` (новый файл)

**Функции:**
- `normalize_text()` - нормализация текста для сравнения
- `normalize_postcode()` - нормализация почтового индекса
- `normalize_telephone()` - нормализация телефонного номера
- `calculate_name_similarity()` - вычисление схожести названий (0.0-1.0)
- `create_matching_keys()` - создание ключей для CQC данных
- `create_staging_keys()` - создание ключей для Staging данных
- `match_cqc_to_staging()` - основной алгоритм сопоставления
- `build_staging_index_with_keys()` - построение индекса для быстрого поиска

**Особенности:**
- Многоуровневая стратегия сопоставления
- Поддержка нечеткого сопоставления названий
- Обработка различных форматов данных

### 2. `services/staging_data_loader.py` (обновлен)

**Изменения:**
- `load_staging_data()` теперь возвращает `List[Dict]` вместо `Dict[str, Dict]`
- Не пропускает записи с пустым `cqc_location_id`
- Сохраняет оригинальную строку в `_original_staging_row` для сопоставления

**Причина:** Поскольку `cqc_location_id` пуст, индексация по нему невозможна. Вместо этого возвращается список, который индексируется в `care_home_matcher`.

### 3. `services/hybrid_data_merger.py` (обновлен)

**Изменения:**
- `merge_cqc_and_staging()` теперь принимает `List[Dict]` вместо `Dict[str, Dict]`
- Использует `care_home_matcher.match_cqc_to_staging()` для сопоставления
- Сначала пытается сопоставить по `location_id` (если есть), затем по полям
- Логирует статистику: сколько сопоставлено по ID, сколько по полям

**Логика:**
```python
# 1. Попытка сопоставления по location_id (если есть в staging)
# 2. Если не найдено - использование field-based matching
# 3. Объединение данных с приоритетом CQC → Staging
```

### 4. `services/csv_care_homes_service.py` (обновлен)

**Изменения:**
- Переименована переменная `staging_index` → `staging_list` для ясности
- Остальная логика без изменений

---

## 🔍 Алгоритм сопоставления

### Нормализация данных

**Название:**
- Приведение к нижнему регистру
- Удаление лишних пробелов
- Удаление спецсимволов (для сравнения)

**Почтовый индекс:**
- Удаление всех пробелов
- Приведение к верхнему регистру
- Пример: `"CV11 4PJ"` → `"CV114PJ"`

**Телефон:**
- Удаление всех нецифровых символов
- Пример: `"09230 33512"` → `"0923033512"`

### Вычисление схожести названий

Используется Jaccard similarity по словам:
```python
similarity = len(words1 & words2) / len(words1 | words2)
```

**Примеры:**
- `"Gildawood Court"` vs `"Gildawood Court"` → 1.0 (точное совпадение)
- `"Gildawood Court"` vs `"Gildawood Court Care Home"` → 0.8 (одно содержит другое)
- `"Radnor House"` vs `"Radnor House Care"` → ~0.67 (частичное совпадение)

---

## 📊 Статистика сопоставления

После объединения логируется:
- Общее количество домов из CQC
- Количество сопоставленных с Staging
  - По `location_id` (если есть)
  - По полям (name+postcode, name+city, fuzzy, telephone)
- Количество домов только из CQC (без данных Staging)

---

## 🧪 Тестирование

### Пример использования:

```python
from services.cqc_data_loader import load_cqc_homes
from services.staging_data_loader import load_staging_data
from services.hybrid_data_merger import merge_cqc_and_staging

# Загрузить данные
cqc_homes = load_cqc_homes()
staging_list = load_staging_data()

# Объединить
merged_homes = merge_cqc_and_staging(cqc_homes, staging_list)

# Результат: список домов с объединенными данными
# Приоритет: CQC → Staging (fallback)
```

### Проверка сопоставления:

```python
from services.care_home_matcher import build_staging_index_with_keys, match_cqc_to_staging

# Построить индекс
staging_index = build_staging_index_with_keys(staging_list)

# Найти соответствие для конкретного дома
cqc_home = cqc_homes[0]
staging_match = match_cqc_to_staging(cqc_home, staging_index)

if staging_match:
    print(f"Найдено соответствие для {cqc_home['name']}")
else:
    print(f"Соответствие не найдено для {cqc_home['name']}")
```

---

## ⚠️ Ограничения

1. **Производительность:** Нечеткое сопоставление требует перебора всех записей Staging для каждого дома CQC. Для больших объемов данных может быть медленным.

2. **Точность:** Нечеткое сопоставление может давать ложные срабатывания, если названия похожи, но это разные дома.

3. **Зависимость от качества данных:** Если в Staging отсутствуют name, postcode, city - сопоставление невозможно.

---

## 🚀 Улучшения в будущем

1. **Кэширование индекса:** Сохранять построенный индекс для повторного использования
2. **Географическое сопоставление:** Использовать координаты (latitude/longitude) для дополнительной проверки
3. **Машинное обучение:** Обучить модель для более точного сопоставления
4. **Параллельная обработка:** Использовать multiprocessing для ускорения нечеткого сопоставления

---

## 📝 Выводы

✅ **Реализовано:**
- Многоуровневый алгоритм сопоставления
- Поддержка нечеткого сопоставления
- Обратная совместимость (поддержка location_id, если есть)

✅ **Результат:**
- Staging данные теперь могут быть объединены с CQC данными
- Fallback логика работает: если нет данных в CQC → используется Staging
- Алгоритм матчинга получает объединенные данные без изменений

