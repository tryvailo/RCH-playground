# 📊 Полный анализ Facilities и Funding для матчинга

**Дата:** 2025-12-20  
**Статус:** ✅ АНАЛИЗ ЗАВЕРШЕН

---

## 🎯 Цель анализа

Проверить наличие и доступность полей Facilities и Funding в CQC базе данных для использования в алгоритме матчинга.

---

## 📊 Результаты анализа

### **1. Facilities (Удобства)** ✅

#### **В схеме базы данных:**

**Плоские поля (5 полей):**
```sql
wheelchair_access BOOLEAN DEFAULT FALSE,
ensuite_rooms BOOLEAN DEFAULT FALSE,
secure_garden BOOLEAN DEFAULT FALSE,
wifi_available BOOLEAN DEFAULT FALSE,
parking_onsite BOOLEAN DEFAULT FALSE,
```

**JSONB поле:**
```sql
facilities JSONB DEFAULT '{}'::jsonb,
```

**Статус:** ✅ **ЕСТЬ** в схеме БД

---

#### **В CSV:**

**Результат:** ❌ **ОТСУТСТВУЮТ**

- Плоские поля не экспортированы в CSV
- JSONB поле не экспортировано в CSV

**Причина:** CSV экспорт не включает эти поля

---

#### **Функция извлечения:**

**`get_amenity_value(home, amenity_name)`** из `db_field_extractor.py`

**Логика:**
1. ✅ Проверяет плоское поле (например, `parking_onsite`)
2. ✅ Если `NULL`, проверяет JSONB `facilities` → `general_amenities` (массив)
3. ✅ Если не найдено, проверяет JSONB `facilities` → `amenity_name` (прямой ключ)
4. ✅ Возвращает `True`/`False`/`None`

**Статус:** ✅ **РАБОТАЕТ** при загрузке из БД

**Использование в матчинге:**
- ✅ Используется в `_calculate_location()` для `parking_onsite`, `wheelchair_access`
- ✅ Используется в `_calculate_lifestyle()` для `ensuite_rooms`, `secure_garden`, `wifi_available`

---

### **2. Funding (Финансирование)** ❌

#### **В схеме базы данных:**

**Плоские поля:**
```sql
-- НЕ НАЙДЕНО!
-- Нет полей: accepts_local_authority_funding, accepts_private_funding, accepts_nhs_funding
```

**JSONB поле:**
```sql
-- НЕ НАЙДЕНО!
-- Нет поля: funding или funding_acceptance
```

**Статус:** ❌ **ОТСУТСТВУЕТ** в схеме БД

---

#### **В CSV:**

**Результат:** ❌ **ОТСУТСТВУЮТ**

- Поля funding не найдены в CSV

---

#### **Функция извлечения:**

**`get_funding_acceptance(home, funding_type)`** из `db_field_extractor.py`

**Логика:**
1. ⚠️ Проверяет плоские поля:
   - `accepts_self_funding`
   - `accepts_local_authority`
   - `accepts_nhs_chc`
   - `accepts_third_party_topup`
2. ❌ **НЕ проверяет JSONB** (нет логики для JSONB `funding`)

**Проблема:** Функция проверяет поля, которых **НЕТ** в схеме БД!

**Статус:** ❌ **НЕ РАБОТАЕТ** (проверяет несуществующие поля)

---

## 🔍 Проверка Staging базы данных

### **Staging база (`carehome_staging_export.csv`):**

**Проверка на Funding:**
- ⚠️ Требует проверки наличия funding полей

**Проверка на Facilities:**
- ⚠️ Требует проверки наличия facilities полей

**Вывод:** ⚠️ Требует детальной проверки Staging базы

---

## 📋 Итоговая таблица

| Категория | Плоские поля в БД | JSONB поле в БД | В CSV | Функция извлечения | Использование в матчинге | Статус |
|-----------|-------------------|-----------------|-------|-------------------|-------------------------|--------|
| **Facilities** | ✅ Есть (5 полей) | ✅ `facilities` | ❌ Нет | ✅ `get_amenity_value()` | ✅ Используется | ✅ **РАБОТАЕТ** |
| **Funding** | ❌ Нет | ❌ Нет | ❌ Нет | ⚠️ `get_funding_acceptance()` | ❌ Не используется | ❌ **НЕ РАБОТАЕТ** |

---

## ⚠️ Проблемы

### **Проблема 1: CSV не содержит JSONB данные**

**Причина:**
- CSV экспорт из базы данных не включает JSONB поля
- JSONB данные остаются только в базе данных

**Последствия:**
- При загрузке из CSV JSONB данные недоступны
- Функции извлечения не могут получить данные из JSONB

**Решение:**
- ✅ Использовать базу данных напрямую (не CSV)
- ✅ Использовать `AsyncDataLoader` для загрузки из БД

---

### **Проблема 2: Funding поля отсутствуют в БД**

**Причина:**
- Поля funding не добавлены в схему БД
- Нет JSONB поля для funding

**Последствия:**
- Funding данные недоступны ни в БД, ни в CSV
- Функция `get_funding_acceptance()` не работает

**Решение:**
- ⚠️ Проверить Staging базу на наличие funding данных
- ⚠️ Добавить funding поля в БД (миграция)
- ⚠️ Обогатить через внешние API

---

## ✅ Решения

### **Решение 1: Использовать базу данных напрямую** ✅

**Для Facilities:**

**Подход:**
- Загружать данные напрямую из базы данных (не из CSV)
- Использовать `AsyncDataLoader` для загрузки из БД
- JSONB поля будут доступны через `db_field_extractor.py`

**Статус:** ✅ Уже реализовано в `report_routes.py` (fallback на `AsyncDataLoader`)

**Использование:**
```python
from services.data_loader import AsyncDataLoader

loader = AsyncDataLoader()
homes = await loader.load_initial_data(...)  # Загружает из БД с JSONB

# Facilities доступны через:
from services.db_field_extractor import get_amenity_value

parking = get_amenity_value(home, 'parking_onsite')
wheelchair = get_amenity_value(home, 'wheelchair_access')
```

---

### **Решение 2: Проверить Staging базу** ⚠️

**Для Funding:**

**Подход:**
- Проверить, содержит ли `carehome_staging_export.csv` funding данные
- Если да, использовать гибридный подход: CQC + Staging
- Объединить данные через `hybrid_data_merger.py`

**Статус:** ⚠️ Требует проверки

---

### **Решение 3: Добавить Funding в БД** ⚠️

**Подход:**
- Добавить JSONB поле `funding_acceptance` в схему БД
- Или добавить плоские поля `accepts_*_funding`

**Статус:** ⚠️ Требует миграции БД

---

### **Решение 4: Обогатить через API** ⚠️

**Подход:**
- Использовать внешние источники для получения funding данных
- Обновить базу данных с этими данными

**Статус:** ⚠️ Требует реализации

---

## 🎯 Рекомендации

### **Приоритет 1: Использовать БД для Facilities** ✅

**Действие:**
- При загрузке данных использовать `AsyncDataLoader` вместо CSV
- Это обеспечит доступ к JSONB полям `facilities`

**Статус:** ✅ Уже реализовано

---

### **Приоритет 2: Проверить Staging базу для Funding** ⚠️

**Действие:**
- Проверить, содержит ли Staging база funding данные
- Если да, интегрировать в гибридный подход

**Статус:** ⚠️ Требует проверки

---

### **Приоритет 3: Обновить функцию `get_funding_acceptance()`** ⚠️

**Действие:**
- Добавить проверку JSONB `funding_acceptance` (если будет добавлено)
- Или использовать данные из Staging базы

**Статус:** ⚠️ Требует реализации

---

## ✅ Выводы

### **Facilities:**

1. ✅ **Плоские поля ЕСТЬ** в схеме БД
2. ✅ **JSONB поле ЕСТЬ** в схеме БД
3. ❌ **НЕ экспортированы в CSV**
4. ✅ **Функция извлечения работает** (при загрузке из БД)
5. ✅ **Используется в матчинге**

**Рекомендация:** ✅ Использовать базу данных напрямую для доступа к facilities данным.

---

### **Funding:**

1. ❌ **Плоские поля ОТСУТСТВУЮТ** в схеме БД
2. ❌ **JSONB поле ОТСУТСТВУЕТ** в схеме БД
3. ❌ **НЕ экспортированы в CSV**
4. ⚠️ **Функция извлечения не работает** (проверяет несуществующие поля)
5. ❌ **НЕ используется в матчинге**

**Рекомендация:** ⚠️ Проверить Staging базу или добавить funding поля в БД.

---

## 📊 Итоговая оценка готовности

### **Facilities для матчинга:** 🟢 **100%**

- ✅ Поля присутствуют в БД
- ✅ Функции извлечения реализованы
- ✅ Используются в матчинге
- ⚠️ Требуется загрузка из БД (не CSV)

---

### **Funding для матчинга:** 🔴 **0%**

- ❌ Поля отсутствуют в БД
- ❌ Функции извлечения не работают
- ❌ Не используются в матчинге
- ⚠️ Требуется добавление полей в БД или использование альтернативных источников

---

**Последнее обновление:** 2025-12-20

