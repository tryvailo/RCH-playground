# ⚠️ Анализ проблемы обновления CQC базы данных

**Дата:** 2025-12-20  
**Статус:** ❌ ПРОБЛЕМА ОБНАРУЖЕНА

---

## 🎯 Задача

**Цель:** Обновить `cqc_carehomes_master_full_data_rows.csv` используя CQC API для заполнения пустых или неправильных полей.

---

## 📊 Текущее состояние файлов

### **Размеры файлов:**

| Файл | Размер | Строки | Статус |
|------|--------|--------|--------|
| `cqc_carehomes_master_full_data_rows.csv` | 15M | ~14,600 | ✅ Оригинальный |
| `cqc_carehomes_master_full_data_rows_UPDATED.csv` | 9.2K | ~20 | ❌ Только тестовые данные |

**Вывод:** Обновленный файл содержит только тестовые данные (20 домов), а не все 14,600 домов!

---

## 🔍 Анализ проблемы

### **Проблема 1: Скрипт создает новый файл вместо обновления оригинального**

**Код в `update_cqc_database.py` (строки 748-752):**
```python
# Save in same directory as original, with _UPDATED suffix
csv_dir = csv_path.parent
csv_stem = csv_path.stem
csv_suffix = csv_path.suffix
output_file = csv_dir / f"{csv_stem}_UPDATED{csv_suffix}"
```

**Результат:**
- ✅ Скрипт создает новый файл: `cqc_carehomes_master_full_data_rows_UPDATED.csv`
- ❌ **НЕ обновляет** оригинальный файл: `cqc_carehomes_master_full_data_rows.csv`

---

### **Проблема 2: Система загрузки использует оригинальный файл**

**Код в `cqc_data_loader.py` (строки 258-261):**
```python
_possible_paths = [
    ... / "cqc_carehomes_master_full_data_rows.csv",  # БЕЗ _UPDATED!
    ...
]
```

**Результат:**
- ✅ Система загружает данные из: `cqc_carehomes_master_full_data_rows.csv`
- ❌ **НЕ загружает** данные из: `cqc_carehomes_master_full_data_rows_UPDATED.csv`

---

### **Проблема 3: Обновленный файл содержит только тестовые данные**

**Причина:**
- Скрипт был запущен с параметром `--limit 20` для тестирования
- Обновлены только первые 20 домов
- Функция `save_homes_to_csv()` сохраняет только обновленные дома, а не все дома из оригинального файла

**Код в `update_cqc_database.py` (строки 351-466):**
```python
def save_homes_to_csv(
    homes: List[Dict],
    output_path: Path,
    original_csv_path: Path
):
    # Сохраняет только homes (обновленные дома), а не все дома из оригинального файла
```

**Результат:**
- ❌ Обновленный файл содержит только 20 домов (тестовые данные)
- ❌ Остальные 14,580 домов **НЕ включены** в обновленный файл

---

## ⚠️ Критические проблемы

### **1. Несоответствие между обновлением и загрузкой:**

- **Скрипт обновления** создает файл: `cqc_carehomes_master_full_data_rows_UPDATED.csv`
- **Система загрузки** использует файл: `cqc_carehomes_master_full_data_rows.csv`

**Результат:** Обновления **НЕ применяются** в системе

---

### **2. Обновленный файл неполный:**

- Обновленный файл содержит только 20 домов (тестовые данные)
- Остальные 14,580 домов отсутствуют

**Результат:** Обновленный файл **НЕ может быть использован** как замена оригинального

---

### **3. Оригинальный файл не обновлен:**

- Оригинальный файл `cqc_carehomes_master_full_data_rows.csv` остался без изменений
- Все обновления находятся в отдельном файле `_UPDATED.csv`

**Результат:** Система продолжает использовать данные без обновлений

---

## 🎯 Что нужно было сделать

### **Вариант 1: Обновить оригинальный файл (РЕКОМЕНДУЕТСЯ)**

**Действие:**
1. Загрузить **ВСЕ** дома из оригинального файла
2. Обновить нужные поля через CQC API
3. Сохранить **ВСЕ** дома обратно в оригинальный файл
4. Создать резервную копию перед обновлением

**Логика:**
```python
# 1. Загрузить ВСЕ дома из оригинального файла
all_homes = load_cqc_homes(csv_path)

# 2. Обновить нужные поля через API
for home in all_homes:
    update_home_from_api(home, ...)

# 3. Сохранить ВСЕ дома обратно в оригинальный файл
save_all_homes_to_csv(all_homes, csv_path, csv_path)  # Тот же файл!
```

---

### **Вариант 2: Использовать обновленный файл**

**Действие:**
1. Загрузить **ВСЕ** дома из оригинального файла
2. Обновить нужные поля через CQC API
3. Сохранить **ВСЕ** дома в новый файл `_UPDATED.csv`
4. Изменить `cqc_data_loader.py` для загрузки из `_UPDATED.csv`

**Логика:**
```python
# 1. Загрузить ВСЕ дома
all_homes = load_cqc_homes(csv_path)

# 2. Обновить
for home in all_homes:
    update_home_from_api(home, ...)

# 3. Сохранить ВСЕ дома в новый файл
save_all_homes_to_csv(all_homes, updated_csv_path, csv_path)

# 4. Изменить cqc_data_loader.py для использования updated_csv_path
```

---

## 📋 Что было сделано неправильно

### **1. Сохранение только обновленных домов:**

**Проблема:**
- Функция `save_homes_to_csv()` сохраняет только дома, которые были обновлены
- Остальные дома не включены в обновленный файл

**Нужно было:**
- Сохранять **ВСЕ** дома из оригинального файла
- Включать обновленные поля для обновленных домов
- Сохранять оригинальные поля для необновленных домов

---

### **2. Создание нового файла вместо обновления оригинального:**

**Проблема:**
- Скрипт создает новый файл `_UPDATED.csv`
- Оригинальный файл остается без изменений
- Система продолжает использовать оригинальный файл

**Нужно было:**
- Обновлять оригинальный файл напрямую
- Или изменить систему загрузки для использования обновленного файла

---

### **3. Тестирование на ограниченном наборе данных:**

**Проблема:**
- Скрипт был запущен с `--limit 20` для тестирования
- Обновлены только 20 домов
- Обновленный файл содержит только эти 20 домов

**Нужно было:**
- После успешного тестирования запустить полное обновление без `--limit`
- Или сохранять все дома, даже если обновлены только некоторые

---

## ✅ Правильный подход

### **Шаг 1: Загрузить ВСЕ дома из оригинального файла**

```python
all_homes = load_cqc_homes(csv_path)  # Все 14,600 домов
```

---

### **Шаг 2: Обновить нужные поля через API**

```python
for home in all_homes:
    update_result = await update_home_from_api(home, cqc_client, fields_to_update)
    # Обновляет только нужные поля, остальные остаются без изменений
```

---

### **Шаг 3: Сохранить ВСЕ дома обратно в файл**

```python
# Вариант A: Обновить оригинальный файл
save_all_homes_to_csv(all_homes, csv_path, csv_path)  # Тот же файл!

# Вариант B: Сохранить в новый файл
save_all_homes_to_csv(all_homes, updated_csv_path, csv_path)  # Новый файл
# Затем изменить cqc_data_loader.py для использования updated_csv_path
```

---

## 📊 Итоговая таблица проблем

| Проблема | Текущее состояние | Нужное состояние |
|----------|-------------------|------------------|
| **Файл обновления** | `_UPDATED.csv` (новый файл) | Оригинальный файл или измененная загрузка |
| **Количество домов** | 20 домов (тестовые) | Все 14,600 домов |
| **Использование системой** | Нет (используется оригинальный) | Да (обновленный файл) |
| **Применение обновлений** | Нет | Да |

---

## 🎯 Рекомендации

### **Приоритет 1: Исправить логику сохранения**

**Действие:**
1. Изменить `save_homes_to_csv()` для сохранения **ВСЕХ** домов
2. Включать обновленные поля для обновленных домов
3. Сохранять оригинальные поля для необновленных домов

---

### **Приоритет 2: Обновить оригинальный файл**

**Действие:**
1. Создать резервную копию оригинального файла
2. Обновить оригинальный файл напрямую
3. Или изменить систему загрузки для использования обновленного файла

---

### **Приоритет 3: Запустить полное обновление**

**Действие:**
1. Запустить скрипт без `--limit` для обновления всех домов
2. Убедиться, что все дома сохранены в обновленный файл
3. Применить обновления в системе

---

**Последнее обновление:** 2025-12-20

