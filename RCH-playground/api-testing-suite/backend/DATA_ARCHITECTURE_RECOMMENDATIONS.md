# Рекомендации по архитектуре данных для MVP

**Дата:** 2025-01-XX  
**Эксперт:** Архитектор данных  
**Контекст:** Bootstrap MVP, 4 источника данных  
**Статус:** ✅ РЕКОМЕНДАЦИИ ГОТОВЫ

---

## 📊 Текущая ситуация

### Источники данных:

1. **CQC (Care Quality Commission)** - официальный источник
   - ✅ Структурированные данные (129 полей)
   - ✅ Авторитетный источник (regulatory)
   - ✅ Полное покрытие UK care homes
   - ✅ Регулярные обновления

2. **carehomes.co.uk** - каталог (парсинг)
   - ⚠️ Неструктурированные данные
   - ⚠️ Цены, отзывы, фото, описания
   - ⚠️ Неполное покрытие

3. **lottie** - каталог (парсинг)
   - ⚠️ Неструктурированные данные
   - ⚠️ Цены, отзывы, фото, описания
   - ⚠️ Неполное покрытие

4. **autumna** - каталог (парсинг)
   - ⚠️ Неструктурированные данные
   - ⚠️ Цены, отзывы, фото, описания
   - ⚠️ Неполное покрытие

### Проблема:

- Разные структуры данных
- Сложный маппинг в одну таблицу
- Разные идентификаторы (нет единого ключа)
- Разное качество данных

---

## 🎯 Рекомендация для MVP: Гибридная архитектура

### Вариант 1: Основная таблица + Вспомогательные таблицы (РЕКОМЕНДУЕТСЯ)

```
┌─────────────────────────────────────────────────────────────┐
│                    care_homes (CQC)                          │
│  ─────────────────────────────────────────────────────────   │
│  • Primary Key: cqc_location_id (authoritative)              │
│  • 78 плоских полей из CQC                                  │
│  • 17 JSONB полей (regulated_activities, service_user_bands)│
│  • Полное покрытие UK care homes                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ 1:N
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              care_homes_catalog_data                        │
│  ─────────────────────────────────────────────────────────   │
│  • Foreign Key: cqc_location_id                            │
│  • source: 'autumna' | 'carehomes_co_uk' | 'lottie'        │
│  • source_id: ID из каталога                               │
│  • match_confidence: 0.0-1.0 (качество матчинга)          │
│  • data: JSONB (все данные из каталога)                    │
│  • last_updated: TIMESTAMP                                 │
└─────────────────────────────────────────────────────────────┘
```

**Преимущества:**
- ✅ Простота: CQC остается основой, каталоги - дополнение
- ✅ Скорость разработки: не нужно сложного маппинга всех полей
- ✅ Гибкость: легко добавлять новые каталоги
- ✅ Качество: CQC как authoritative source
- ✅ Производительность: основной запрос по CQC, каталоги - по необходимости

**Недостатки:**
- ⚠️ Нужен матчинг по имени/адресу (но это проще, чем полный маппинг)
- ⚠️ Дублирование данных (но это нормально для MVP)

---

### Вариант 2: Единая таблица с маппингом (НЕ РЕКОМЕНДУЕТСЯ для MVP)

```
┌─────────────────────────────────────────────────────────────┐
│                    care_homes (unified)                      │
│  ─────────────────────────────────────────────────────────   │
│  • Все поля из всех источников                              │
│  • Сложный маппинг полей                                    │
│  • Много NULL значений                                     │
│  • Сложная логика приоритетов                               │
└─────────────────────────────────────────────────────────────┘
```

**Проблемы:**
- ❌ Сложность: нужно маппить все поля из всех источников
- ❌ Время разработки: долго и сложно
- ❌ Поддержка: сложно обновлять при изменении источников
- ❌ Качество: много NULL, сложная логика приоритетов

---

## 🏗️ Рекомендуемая архитектура для MVP

### Структура БД:

```sql
-- ========================================================================
-- ОСНОВНАЯ ТАБЛИЦА: care_homes (CQC)
-- ========================================================================
CREATE TABLE care_homes (
    id SERIAL PRIMARY KEY,
    cqc_location_id TEXT UNIQUE NOT NULL,  -- Primary identifier
    -- ... все поля из CQC (78 плоских + 17 JSONB)
    -- ... как в текущей структуре
);

-- ========================================================================
-- ВСПОМОГАТЕЛЬНАЯ ТАБЛИЦА: care_homes_catalog_data
-- ========================================================================
CREATE TABLE care_homes_catalog_data (
    id SERIAL PRIMARY KEY,
    cqc_location_id TEXT NOT NULL REFERENCES care_homes(cqc_location_id),
    source TEXT NOT NULL,  -- 'autumna', 'carehomes_co_uk', 'lottie'
    source_id TEXT,  -- ID из каталога
    source_url TEXT,  -- URL страницы каталога
    
    -- Качество матчинга
    match_confidence NUMERIC(3,2) DEFAULT 0.0,  -- 0.0-1.0
    match_method TEXT,  -- 'name_exact', 'name_fuzzy', 'address', 'manual'
    
    -- Данные из каталога (JSONB)
    data JSONB DEFAULT '{}'::jsonb,
    -- Структура data:
    -- {
    --   "pricing": {...},
    --   "reviews": {...},
    --   "photos": [...],
    --   "description": "...",
    --   "amenities": [...],
    --   "availability": {...},
    --   "contact": {...}
    -- }
    
    -- Метаданные
    last_updated TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT TRUE,
    
    -- Уникальность: один каталог = одна запись на дом
    UNIQUE(cqc_location_id, source)
);

-- Индексы для производительности
CREATE INDEX idx_catalog_data_cqc_location_id ON care_homes_catalog_data(cqc_location_id);
CREATE INDEX idx_catalog_data_source ON care_homes_catalog_data(source);
CREATE INDEX idx_catalog_data_match_confidence ON care_homes_catalog_data(match_confidence DESC);
CREATE INDEX idx_catalog_data_data_gin ON care_homes_catalog_data USING GIN(data);
```

---

## 🔄 Процесс матчинга каталогов с CQC

### Шаг 1: Матчинг по имени и адресу

```python
def match_catalog_to_cqc(catalog_home: dict, cqc_homes: list) -> dict:
    """
    Матчинг дома из каталога с CQC базой.
    
    Возвращает:
    {
        'cqc_location_id': '1-123456789',
        'match_confidence': 0.95,
        'match_method': 'name_exact'
    }
    """
    # 1. Exact name match
    for cqc_home in cqc_homes:
        if normalize_name(catalog_home['name']) == normalize_name(cqc_home['name']):
            if same_postcode(catalog_home['postcode'], cqc_home['postcode']):
                return {
                    'cqc_location_id': cqc_home['cqc_location_id'],
                    'match_confidence': 0.95,
                    'match_method': 'name_exact'
                }
    
    # 2. Fuzzy name match
    for cqc_home in cqc_homes:
        similarity = fuzzy_match(
            normalize_name(catalog_home['name']),
            normalize_name(cqc_home['name'])
        )
        if similarity > 0.85:
            if same_city(catalog_home['city'], cqc_home['city']):
                return {
                    'cqc_location_id': cqc_home['cqc_location_id'],
                    'match_confidence': similarity,
                    'match_method': 'name_fuzzy'
                }
    
    # 3. Address match
    for cqc_home in cqc_homes:
        if same_address(catalog_home['address'], cqc_home['address']):
            return {
                'cqc_location_id': cqc_home['cqc_location_id'],
                'match_confidence': 0.80,
                'match_method': 'address'
            }
    
    # 4. No match
    return {
        'cqc_location_id': None,
        'match_confidence': 0.0,
        'match_method': 'no_match'
    }
```

---

### Шаг 2: Загрузка данных из каталогов

```python
async def load_catalog_data(source: str, catalog_homes: list):
    """
    Загрузить данные из каталога в care_homes_catalog_data.
    """
    # Получить все CQC дома
    cqc_homes = await get_all_cqc_homes()
    
    for catalog_home in catalog_homes:
        # Матчинг с CQC
        match = match_catalog_to_cqc(catalog_home, cqc_homes)
        
        if match['match_confidence'] >= 0.80:  # Минимальный порог
            # Сохранить в БД
            await db.execute("""
                INSERT INTO care_homes_catalog_data (
                    cqc_location_id,
                    source,
                    source_id,
                    source_url,
                    match_confidence,
                    match_method,
                    data,
                    last_updated
                ) VALUES ($1, $2, $3, $4, $5, $6, $7, NOW())
                ON CONFLICT (cqc_location_id, source)
                DO UPDATE SET
                    data = EXCLUDED.data,
                    match_confidence = EXCLUDED.match_confidence,
                    last_updated = NOW()
            """, (
                match['cqc_location_id'],
                source,
                catalog_home.get('id'),
                catalog_home.get('url'),
                match['match_confidence'],
                match['match_method'],
                json.dumps(catalog_home)  # Все данные в JSONB
            ))
```

---

## 📊 Использование данных в приложении

### Запрос данных для матчинга:

```python
async def get_care_home_with_catalogs(cqc_location_id: str) -> dict:
    """
    Получить дом из CQC + данные из всех каталогов.
    """
    # Основные данные из CQC
    cqc_home = await db.fetch_one("""
        SELECT * FROM care_homes
        WHERE cqc_location_id = $1
    """, cqc_location_id)
    
    # Данные из каталогов
    catalog_data = await db.fetch_all("""
        SELECT source, data, match_confidence
        FROM care_homes_catalog_data
        WHERE cqc_location_id = $1
        AND is_active = TRUE
        ORDER BY match_confidence DESC
    """, cqc_location_id)
    
    # Объединить данные
    result = dict(cqc_home)
    result['catalog_data'] = {
        catalog['source']: {
            'data': catalog['data'],
            'confidence': catalog['match_confidence']
        }
        for catalog in catalog_data
    }
    
    return result
```

---

## 🎯 Преимущества для MVP

### 1. Простота разработки ✅

- **CQC как основа:** Не нужно маппить все поля из всех источников
- **JSONB для каталогов:** Гибкая структура, легко добавлять новые поля
- **Матчинг по имени/адресу:** Проще, чем полный маппинг полей

### 2. Скорость разработки ✅

- **Быстрый старт:** Можно начать с CQC, каталоги добавить позже
- **Итеративная разработка:** Добавлять каталоги по одному
- **Меньше тестирования:** Меньше логики маппинга = меньше багов

### 3. Гибкость ✅

- **Легко добавлять каталоги:** Просто новая запись в `care_homes_catalog_data`
- **Разные структуры:** JSONB принимает любую структуру
- **Приоритеты:** Можно выбирать лучший источник для каждого поля

### 4. Качество данных ✅

- **CQC как authoritative:** Всегда есть базовая информация
- **Каталоги как дополнение:** Улучшают данные, но не критичны
- **Матчинг с confidence:** Можно фильтровать по качеству

### 5. Производительность ✅

- **Быстрые запросы:** Основной запрос по CQC (индексирован)
- **Ленивая загрузка:** Каталоги загружаются только при необходимости
- **Кэширование:** Можно кэшировать объединенные данные

---

## 📋 План внедрения для MVP

### Фаза 1: CQC только (Week 1-2)

```sql
-- Только care_homes (CQC)
-- Все поля из CQC
-- Работает матчинг
```

**Результат:** Работающий продукт с CQC данными.

---

### Фаза 2: Добавить 1 каталог (Week 3-4)

```sql
-- Добавить care_homes_catalog_data
-- Матчинг Autumna с CQC
-- Использовать данные из Autumna для:
--   - Цены (если нет в CQC)
--   - Отзывы
--   - Фото
```

**Результат:** Улучшенные данные для части домов.

---

### Фаза 3: Добавить остальные каталоги (Week 5-6)

```sql
-- Добавить carehomes.co.uk
-- Добавить lottie
-- Логика приоритетов (какой каталог использовать)
```

**Результат:** Максимальное покрытие данных.

---

## 🔧 Логика приоритетов данных

### Для каждого поля:

```python
def get_field_value(cqc_location_id: str, field: str) -> Any:
    """
    Получить значение поля с учетом приоритетов источников.
    """
    # 1. CQC (всегда есть)
    cqc_value = get_cqc_value(cqc_location_id, field)
    if cqc_value:
        return cqc_value
    
    # 2. Каталоги (по приоритету)
    catalogs = get_catalog_data(cqc_location_id, order_by='match_confidence')
    
    for catalog in catalogs:
        value = catalog['data'].get(field)
        if value:
            return value
    
    return None
```

### Приоритеты для разных полей:

| Поле | Приоритет 1 | Приоритет 2 | Приоритет 3 |
|------|-------------|-------------|-------------|
| **Цены** | CQC (если есть) | Autumna | carehomes.co.uk |
| **Отзывы** | carehomes.co.uk | Autumna | lottie |
| **Фото** | Autumna | lottie | carehomes.co.uk |
| **Описание** | Autumna | carehomes.co.uk | lottie |
| **Удобства** | CQC | Autumna | carehomes.co.uk |

---

## ⚠️ Потенциальные проблемы и решения

### Проблема 1: Неточный матчинг

**Решение:**
- Использовать `match_confidence` для фильтрации
- Ручная проверка для `match_confidence < 0.90`
- Логирование всех матчей для улучшения алгоритма

---

### Проблема 2: Дублирование данных

**Решение:**
- Это нормально для MVP
- Можно дедуплицировать позже (если нужно)
- JSONB позволяет хранить все версии

---

### Проблема 3: Разные структуры JSONB

**Решение:**
- Нормализовать при загрузке (если возможно)
- Использовать адаптеры для каждого каталога
- Документировать структуру каждого каталога

---

## 📊 Метрики успеха

### Для MVP:

1. **Покрытие данных:**
   - CQC: 100% домов
   - Autumna: ~60-70% домов (матчинг)
   - carehomes.co.uk: ~50-60% домов
   - lottie: ~40-50% домов

2. **Качество матчинга:**
   - `match_confidence >= 0.90`: ~80% матчей
   - `match_confidence >= 0.80`: ~90% матчей
   - `match_confidence < 0.80`: ~10% (требуют ручной проверки)

3. **Производительность:**
   - Запрос CQC: < 50ms
   - Запрос + каталоги: < 200ms
   - Матчинг 1000 домов: < 5 минут

---

## ✅ Итоговая рекомендация

### Для MVP (Bootstrap модель):

**✅ РЕКОМЕНДУЕТСЯ: Гибридная архитектура**

1. **Основная таблица:** `care_homes` (CQC)
   - Все поля из CQC
   - Authoritative source
   - Полное покрытие

2. **Вспомогательная таблица:** `care_homes_catalog_data`
   - Связь через `cqc_location_id`
   - JSONB для гибкости
   - Матчинг по имени/адресу

3. **Логика приоритетов:**
   - CQC → Autumna → carehomes.co.uk → lottie
   - По полям (цены, отзывы, фото)

**Преимущества:**
- ✅ Простота разработки
- ✅ Скорость внедрения
- ✅ Гибкость
- ✅ Качество данных
- ✅ Масштабируемость

**Когда переходить на единую таблицу:**
- После MVP, когда понятны все требования
- Когда есть ресурсы на сложный маппинг
- Когда нужно оптимизировать производительность

---

## 📝 Следующие шаги

1. ✅ Создать таблицу `care_homes_catalog_data`
2. ✅ Реализовать матчинг по имени/адресу
3. ✅ Загрузить данные из Autumna (первый каталог)
4. ✅ Протестировать матчинг и качество данных
5. ✅ Добавить остальные каталоги
6. ✅ Реализовать логику приоритетов

---

**Статус:** ✅ РЕКОМЕНДАЦИИ ГОТОВЫ ДЛЯ ВНЕДРЕНИЯ

