# Quick Start Guide

## 🚀 Быстрый старт за 5 минут

### Шаг 1: Установка зависимостей

**Backend:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

**Frontend:**
```bash
cd frontend
npm install
```

### Шаг 2: Настройка окружения

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```bash
cp .env.example .env
```

Минимальная конфигурация для начала работы:
```env
# FSA не требует ключей - работает сразу
# CQC можно использовать без partner code
```

### Шаг 3: Запуск приложения

**Терминал 1 - Backend:**
```bash
cd backend
uvicorn main:app --reload --port 8000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

### Шаг 4: Откройте браузер

Перейдите на `http://localhost:3000`

### Шаг 5: Первый тест

1. Перейдите на `/config` и настройте API ключи (можно начать только с FSA - он бесплатный)
2. Перейдите на `/test`
3. Выберите "FSA" в списке API
4. Используйте тестовые данные или введите свои
5. Нажмите "Run Comprehensive Test"

## 📝 Минимальные требования для тестирования

Для начала работы достаточно настроить хотя бы один API:

- **FSA** - работает без ключей ✅
- **CQC** - работает без partner code (с ограничениями) ✅

Остальные API требуют ключей, но можно тестировать по одному.

## 🔑 Получение API ключей

См. `API-Credentials-Guide.md` в корне проекта для детальных инструкций по получению каждого ключа.

## ⚠️ Troubleshooting

**Backend не запускается:**
- Проверьте Python версию: `python --version` (нужна 3.9+)
- Убедитесь что порт 8000 свободен

**Frontend не запускается:**
- Проверьте Node версию: `node --version` (нужна 18+)
- Удалите `node_modules` и запустите `npm install` снова

**API тесты не работают:**
- Проверьте ключи в `.env`
- Убедитесь что backend запущен
- Проверьте консоль браузера на ошибки

## 📚 Следующие шаги

1. Настройте все API ключи в `/config`
2. Запустите comprehensive test на `/test`
3. Изучите результаты на `/results/:jobId`
4. Посмотрите аналитику на `/analytics`

---

**Готово!** Теперь вы можете тестировать все API проекта RightCareHome.

