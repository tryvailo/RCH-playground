# Free Report Viewer Module

Модуль для генерации бесплатных отчётов о домах престарелых на основе анкетных данных.

## ⚠️ Fair Cost Gap - Важная информация

**Fair Cost Gap** = разница между рыночной ценой (Lottie average или scraped) и MSIF fair cost lower bound (из БД msif_fees_2025).

### Формула расчёта:
- `gap_week = market_price - msif_lower_bound`
- `gap_year = gap_week * 52`
- `gap_5year = gap_year * 5`

Где:
- `market_price` = средняя рыночная цена (из Lottie API или scraped данных)
- `msif_lower_bound` = MSIF fair cost lower bound из таблицы `msif_fees_2025` (колонка `nursing_median` или `residential_median` для соответствующего `local_authority`)

Это **ОБЯЗАТЕЛЬНЫЙ эмоциональный блок** в отчёте (красный цвет #EF4444, огромные цифры).

## 📁 Структура модуля

```
src/free_report_viewer/
├── __init__.py          # Инициализация модуля
├── models.py            # Pydantic модели данных
├── api.py               # API клиент для взаимодействия с FastAPI backend
├── viewer.py            # Streamlit интерфейс
├── tests/               # Тесты
│   ├── __init__.py
│   ├── test_models.py
│   └── test_api.py
└── README.md            # Документация
```

## 🚀 Быстрый старт

### 1. Запуск FastAPI Backend

```bash
cd api-testing-suite/backend
uvicorn main:app --reload --port 8000
```

### 2. Запуск Streamlit приложения

**Вариант A: Использование основного app.py**

```bash
streamlit run app.py
```

**Вариант B: Использование pages/**

```bash
streamlit run pages/1_Free_Report_Viewer.py
```

Приложение будет доступно по адресу `http://localhost:8501`

## 📋 Использование

### 1. Выбор анкеты

- Выберите одну из примеров анкет из выпадающего списка в сайдбаре
- Или загрузите свой JSON файл через `st.file_uploader`

### 2. Просмотр данных анкеты

После выбора файла отображается карточка с ключевыми данными:
- 📍 Postcode
- 💰 Weekly Budget
- 🏥 Care Type
- 📊 CHC Probability

### 3. Генерация отчёта

Нажмите кнопку **"🚀 Generate Report"** для создания отчёта.

Отчёт включает:
- **3 рекомендованных дома престарелых** с детальной информацией
- **Обязательный блок Fair Cost Gap** с анализом разрыва в финансировании

## 📊 Формат данных

### QuestionnaireResponse

```json
{
  "postcode": "SW1A 1AA",
  "budget": 1200.0,
  "care_type": "residential",
  "chc_probability": 35.5,
  "address": "Westminster",
  "city": "London"
}
```

### Примеры анкет

Примеры находятся в `data/sample_questionnaires/`:
- `questionnaire_1.json` - London, residential care
- `questionnaire_2.json` - Manchester, nursing care
- `questionnaire_3.json` - Birmingham, dementia care

## 🔌 API Endpoint

### POST /api/free-report

**Request Body:**
```json
{
  "postcode": "SW1A 1AA",
  "budget": 1200.0,
  "care_type": "residential",
  "chc_probability": 35.5
}
```

**Response:**
```json
{
  "questionnaire": {...},
  "care_homes": [
    {
      "name": "Sunshine Care Home",
      "address": "123 High Street",
      "postcode": "SW1A 1AA",
      "weekly_cost": 1080.0,
      "care_types": ["residential", "respite"],
      "rating": "Good",
      "distance_km": 2.5,
      "features": ["Garden", "Activities", "24/7 Care"]
    },
    ...
  ],
  "fair_cost_gap": {
    "gap_week": 300.0,
    "gap_year": 15600.0,
    "gap_5year": 78000.0,
    "market_price": 1200.0,
    "msif_lower_bound": 900.0,
    "local_authority": "Westminster",
    "care_type": "residential",
    "explanation": "...",
    "recommendations": [...]
  },
  "generated_at": "2024-01-01T12:00:00",
  "report_id": "uuid-here"
}
```

## 🎨 Стилизация

Приложение использует премиум дизайн с:
- **Цвета:**
  - `#1E2A44` - Тёмно-синий (основной)
  - `#10B981` - Зелёный (акцент)
  - `#EF4444` - Красный (Fair Cost Gap)
  
- **Шрифты:**
  - Inter - основной текст
  - Poppins - заголовки

- **Компоненты:**
  - Карточки с тенями и закруглениями
  - Плавные переходы и hover-эффекты
  - Адаптивная вёрстка

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest src/free_report_viewer/tests/

# Запуск конкретного теста
pytest src/free_report_viewer/tests/test_models.py -v

# С покрытием
pytest src/free_report_viewer/tests/ --cov=src/free_report_viewer
```

## 📝 Зависимости

- `streamlit` - веб-интерфейс
- `pydantic` - валидация данных
- `httpx` - HTTP клиент
- `pytest` - тестирование
- `psycopg2-binary` - PostgreSQL драйвер для доступа к БД msif_fees_2025

## 🔧 Конфигурация

### Изменение URL Backend

В `viewer.py` или через переменные окружения:

```python
api_client = FreeReportAPIClient(base_url="http://your-backend:8000")
```

### Подключение к БД msif_fees_2025

Для работы с реальными данными MSIF необходимо настроить подключение к PostgreSQL:

```bash
# В .env файле или переменных окружения
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
```

Если БД недоступна, используется fallback с mock данными (см. `db_utils.py`).

## 📚 Дополнительная информация

- Модели данных определены в `models.py`
- API клиент в `api.py`
- Streamlit интерфейс в `viewer.py`
- Примеры анкет в `data/sample_questionnaires/`

## ⚠️ Важные замечания

1. **Fair Cost Gap блок обязателен** - всегда включается в отчёт
2. **Минимум 3 дома** - отчёт всегда содержит минимум 3 рекомендованных дома
3. **Backend должен быть запущен** - перед генерацией отчёта убедитесь, что FastAPI сервер работает

## 🐛 Troubleshooting

### Ошибка подключения к API

```
Error generating report: Connection refused
```

**Решение:** Убедитесь, что FastAPI backend запущен на `http://localhost:8000`

### Ошибка парсинга JSON

```
Error parsing questionnaire: ...
```

**Решение:** Проверьте формат JSON файла. Должны быть поля: `postcode` (обязательно), `budget`, `care_type`, `chc_probability`

### Модуль не найден

```
ModuleNotFoundError: No module named 'free_report_viewer'
```

**Решение:** Убедитесь, что `src/` добавлен в `PYTHONPATH` или используйте относительные импорты

