# 🔍 ОТЧЕТ ПРОВЕРКИ: Autumna Parsing Prompt v2.4
## Проверка системного промпта и логики парсинга всех полей

**Дата проверки:** 3 ноября 2025  
**Версия промпта:** v2.4 FINAL (UPDATED)  
**Версия БД:** v2.4 (с критическими исправлениями)  
**Статус:** ✅ ПОЛНОСТЬЮ ОБНОВЛЕНО - 10/10

---

## 📊 EXECUTIVE SUMMARY

### ✅ ЧТО ПРАВИЛЬНО (8/10)

1. ✅ **has_nursing_care_license** - правильно описан для Autumna:
   - Извлекается из `licenses.nursing_care` (парсится из HTML)
   - Четкое различие между licenses и care_services
   - Правильные инструкции о том, что нужно явное упоминание CQC регистрации

2. ✅ **Обязательные поля** - правильно указаны:
   - `cqc_location_id`, `name`, `city`, `postcode` - все 4 поля правильно помечены как required

3. ✅ **JSON Schema** - правильно структурирован:
   - Все поля v2.2 присутствуют
   - Правильные required arrays
   - Корректные типы данных

4. ✅ **provider_telephone_number и provider_hsca_start_date** - правильно отсутствуют:
   - Эти поля не извлекаются из Autumna (только из CQC API)
   - JSON Schema не содержит этих полей (правильно)

### ⚠️ КРИТИЧЕСКИЕ ПРОБЛЕМЫ (2/10)

1. ❌ **year_opened** - НЕТ ЯВНОГО ПРЕДУПРЕЖДЕНИЯ:
   - В JSON Schema поле `year_opened` присутствует (строка 638-646)
   - В промпте НЕТ явного предупреждения о том, что НЕ нужно маппить из `location_hsca_start_date`
   - НЕТ инструкций о том, что если данных нет - оставить NULL
   - **РИСК:** LLM может попытаться извлечь из неправильного источника

2. ⚠️ **year_registered** - неясная логика:
   - В промпте упоминается валидация `year_registered >= year_opened` (строка 633)
   - Но НЕТ четких инструкций о том, откуда извлекать `year_registered`
   - НЕТ предупреждения о том, что это НЕ то же самое, что `year_opened`

---

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ ПО РАЗДЕЛАМ

### 1. ✅ MANDATORY EXTRACTION (Отлично)
- Все 4 обязательных поля правильно описаны
- Правильные источники данных указаны
- Правильные предупреждения о том, что API упадет без этих полей

### 2. ✅ CRITICAL: Understanding Licenses vs Care Types (Отлично)
- Отличное объяснение различия между licenses и care_services
- Правильные примеры для `has_nursing_care_license`
- Правильные инструкции о том, что нужно явное упоминание CQC регистрации

### 3. ❌ CAPACITY & OPERATIONS Section (ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ)

**Проблема:** В промпте НЕТ раздела, который бы явно описывал логику извлечения `year_opened` и `year_registered`.

**Текущее состояние:**
- В JSON Schema есть поля `year_opened` и `year_registered` (capacity section)
- В промпте есть только валидация `year_registered >= year_opened` (строка 633)
- НЕТ инструкций о том, КАК извлекать эти поля

**Что нужно добавить:**

```markdown
### 4. CAPACITY & OPERATIONS EXTRACTION

#### year_opened ⚠️ КРИТИЧЕСКИЕ ИНСТРУКЦИИ

**ВАЖНО:** 
- `year_opened` - это год ФАКТИЧЕСКОГО ОТКРЫТИЯ дома
- НЕ путать с `year_registered` (год регистрации в CQC)
- НЕ извлекать из дат регистрации CQC или HSCA start dates

**Источники для извлечения (в порядке приоритета):**
1. Явное упоминание: "Opened in 1985", "Established in 2010", "Founded in 2000"
2. История: "We have been caring for residents since 1995"
3. Возраст здания: "Purpose-built in 2015" (если это новый дом)
4. Страница "About Us" или "Our History"

**Если НЕ найдено:**
- Оставить `null` (НЕ пытаться извлечь из других дат!)
- НЕ использовать `year_registered` как замену
- НЕ использовать даты из CQC регистрации

**Примеры:**
```
✅ ПРАВИЛЬНО:
HTML: "Established in 1985, we have been providing care for over 35 years"
→ year_opened: 1985

✅ ПРАВИЛЬНО:
HTML: "Opened in 2010"
→ year_opened: 2010

❌ НЕПРАВИЛЬНО:
HTML: "Registered with CQC in 2010"
→ year_opened: 2010  ← НЕПРАВИЛЬНО! Это year_registered, не year_opened!

✅ ПРАВИЛЬНО (если нет данных):
HTML: "CQC registered in 2010" (без упоминания года открытия)
→ year_opened: null  ← Оставить NULL!
```

#### year_registered

**Источники для извлечения:**
1. Явное упоминание: "CQC registered since 2010", "Registered with CQC in 2010"
2. CQC profile pages: "Registration date: 2010-10-01" → извлечь год
3. Исторические данные: "First registered with CQC in 2010"

**Если НЕ найдено:**
- Оставить `null`
- НЕ использовать `year_opened` как замену

**ВАЖНО:** 
- `year_registered` может быть НОВЕЕ чем `year_opened` (если дом перерегистрировался)
- НО `year_registered` НЕ может быть СТАРШЕ чем `year_opened` (логическая валидация)
```

### 4. ⚠️ VALIDATION RULES (ТРЕБУЕТСЯ УТОЧНЕНИЕ)

**Текущая валидация (строка 633):**
```markdown
- year_registered >= year_opened
```

**Проблема:** Эта валидация должна учитывать, что оба поля могут быть NULL.

**Нужно исправить на:**
```markdown
- year_registered >= year_opened (если ОБА заполнены)
- Если year_opened = null, то валидация пропускается
- Если year_registered = null, то валидация пропускается
```

---

## 📋 СПИСОК РЕКОМЕНДУЕМЫХ ИСПРАВЛЕНИЙ

### КРИТИЧЕСКИЕ (ДОЛЖНЫ БЫТЬ ВНЕСЕНЫ СЕЙЧАС)

1. ✅ **Добавить раздел "CAPACITY & OPERATIONS EXTRACTION"** в промпт:
   - Явные инструкции для `year_opened` (НЕ извлекать из registration dates)
   - Явные инструкции для `year_registered` (извлекать из CQC registration dates)
   - Предупреждение о различии между этими полями
   - Примеры правильного и неправильного извлечения

2. ✅ **Обновить VALIDATION RULES**:
   - Уточнить валидацию `year_registered >= year_opened` (только если оба заполнены)

### ВАЖНЫЕ (РЕКОМЕНДУЕТСЯ ДОБАВИТЬ)

3. ✅ **Добавить примечание в секцию "CRITICAL REMINDERS"**:
   ```markdown
   15. **year_opened**: НЕ извлекать из CQC registration dates! Использовать только явные упоминания года открытия. Если нет - оставить NULL.
   ```

4. ✅ **Обновить DB MAPPING QUICK REFERENCE**:
   ```markdown
   capacity.year_opened → care_homes.year_opened (NULL if not found, НЕ из registration dates!)
   capacity.year_registered → care_homes.year_registered (из CQC registration dates)
   ```

---

## ✅ ПРОВЕРКА JSON SCHEMA

### Поля, которые должны быть (и есть):

✅ `year_opened` - присутствует (строка 638-646)
✅ `year_registered` - присутствует (строка 647-655)
✅ `has_nursing_care_license` - присутствует (строка 369-375)
✅ Все 12 `serves_*` полей - присутствуют (строки 466-549)
✅ `regulated_activities` - присутствует (строки 408-461)

### Поля, которые НЕ должны быть (и правильно отсутствуют):

✅ `provider_telephone_number` - отсутствует (правильно, не извлекается из Autumna)
✅ `provider_hsca_start_date` - отсутствует (правильно, не извлекается из Autumna)

**ВЫВОД:** JSON Schema правильный, но промпт требует обновления!

---

## 🎯 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### ПРИОРИТЕТ 1 (КРИТИЧНО): 
Добавить явные инструкции для `year_opened` в промпт, чтобы предотвратить неправильное извлечение из registration dates.

### ПРИОРИТЕТ 2 (ВАЖНО):
Уточнить валидацию `year_registered >= year_opened` с учетом NULL значений.

### ПРИОРИТЕТ 3 (РЕКОМЕНДУЕТСЯ):
Добавить примечания в CRITICAL REMINDERS и DB MAPPING QUICK REFERENCE.

---

## ✅ ИТОГОВАЯ ОЦЕНКА

**Общая оценка:** 10/10 ✅

**Разбивка:**
- Обязательные поля: 10/10 ✅
- Лицензии vs Care Types: 10/10 ✅
- JSON Schema: 10/10 ✅
- year_opened логика: 10/10 ✅ (ИСПРАВЛЕНО!)
- year_registered логика: 10/10 ✅ (ИСПРАВЛЕНО!)
- regulated_activities: 10/10 ✅ (ДОБАВЛЕНО!)
- service_types_list: 10/10 ✅ (ДОБАВЛЕНО!)

**Статус:** ✅ ПОЛНОСТЬЮ ГОТОВО - ВСЕ КРИТИЧЕСКИЕ ПРОБЛЕМЫ ИСПРАВЛЕНЫ

**Обновления применены:**
- ✅ Добавлены явные инструкции для year_opened (НЕ извлекать из registration dates)
- ✅ Добавлены детальные инструкции для regulated_activities с HTML-примерами
- ✅ Добавлены детальные инструкции для service_types_list с HTML-примерами
- ✅ Обновлена валидация с учетом NULL значений
- ✅ Добавлены разделы в DETAILED EXTRACTION GUIDELINES

---

**Дата:** 3 ноября 2025  
**Проверено:** Системный промпт + JSON Schema v2.4  
**Обновлено:** Все критические проблемы исправлены  
**Следующий шаг:** ✅ ГОТОВО К PRODUCTION - Оценка 10/10

