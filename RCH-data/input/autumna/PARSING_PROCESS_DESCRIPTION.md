# 📋 ОПИСАНИЕ ПРОЦЕССА ПАРСИНГА КАРТОЧЕК AUTUMNA С OPENAI

**Дата:** 3 ноября 2025  
**Версия:** v2.4  
**Статус:** ✅ Production Ready

---

## 🎯 ОБЩАЯ АРХИТЕКТУРА

Процесс парсинга организован по **staging-подходу** с промежуточным хранилищем. Это позволяет многократно переобрабатывать данные без повторной загрузки HTML, что критично для итеративного улучшения качества парсинга.

Процесс состоит из **трех последовательных фаз**:

---

## 🔄 ФАЗА 1: ЗАГРУЗКА HTML (выполняется один раз)

### Что происходит:
- **Источник данных:** Firecrawl API (версия v2.5) для получения HTML страниц карточек домов престарелых с сайта autumna.co.uk
- **Действие:** HTML контент сохраняется в таблице `autumna_staging` в поле `html_content`
- **Особенность:** HTML сохраняется один раз, чтобы не скачивать повторно при итерациях

### Технические детали:
- Используется Firecrawl API v2.5 с semantic index cache для ускорения
- HTML сохраняется полностью в staging таблице
- Извлекается `cqc_location_id` из URL для быстрого поиска
- Время: 10-15 минут для 300 страниц
- **Стоимость:** ~$15 за 300 страниц (выполняется один раз!)

---

## 🤖 ФАЗА 2: ПАРСИНГ ЧЕРЕЗ OPENAI LLM (можно повторять многократно)

### Что происходит:
- **Модель:** GPT-4o (`gpt-4o-2024-08-06`)
- **Входные данные:** `html_content` из staging таблицы
- **Промпт:** Системный промпт из файла `AUTUMNA_PARSING_PROMPT_v2_4.md` с детальными инструкциями по извлечению данных
- **JSON Schema:** Строгая валидация через `response_format_v2_4.json` (188 полей)
- **Технология:** OpenAI Structured Outputs с `response_format.type = "json_schema"`

### Результат парсинга:
Структурированный JSON с **188 полями**, включая:
- Идентификаторы (CQC ID, название, провайдер)
- Контакты (телефон, email, сайт)
- Локация (адрес, координаты, регион)
- Типы ухода (residential, nursing, dementia, respite)
- Лицензии CQC (5 типов)
- Ценообразование (weekly fees с диапазонами FROM/TO)
- Медицинские специализации (70+ условий)
- Удобства и услуги
- CQC рейтинги
- И многое другое...

### Обязательные поля:
Система требует извлечения **4 критических полей**, иначе OpenAI API отклоняет ответ:
1. `cqc_location_id` (CQC Location ID) - формат `1-XXXXXXXXXX`
2. `name` (Название дома)
3. `city` (Город)
4. `postcode` (Почтовый индекс)

### Метаданные:
Извлекаются дополнительные метаданные:
- `extraction_confidence` ('high', 'medium', 'low')
- `data_quality_score` (0-100+)
- `is_dormant` (флаг закрытого дома)

### Сохранение результатов:
- Результат сохраняется в `parsed_json` в staging таблице
- Сохраняется версия промпта (`llm_prompt_version`) для отслеживания эффективности разных версий
- Обновляются метаданные качества данных

### Технические детали:
- Обработка батчами (по умолчанию 25 записей)
- Задержка между запросами для rate limiting
- Обработка ошибок с сохранением в `parsing_errors`
- Возможность повторной обработки через флаг `needs_reparse`
- Время: 20-30 минут для 300 страниц
- **Стоимость:** ~$1.62 за 300 страниц (можно повторять многократно, HTML уже есть!)

---

## 🔄 ФАЗА 3: МАППИНГ В ФИНАЛЬНУЮ БД

### Что происходит:
- **Преобразование:** 188 полей JSON → 93 поля БД `care_homes`
- **Валидация:** Проверка критических полей, координат, диапазонов
- **Нормализация:** Использование SQL функций (`safe_latitude`, `normalize_uk_postcode`, `normalize_phone` и др.)
- **Quality Score:** Автоматическое принятие решений о вставке на основе качества данных

### Технические детали:
- Время: 1-2 минуты для 300 записей
- **Стоимость:** $0 (только локальная обработка)

---

## 🔑 КЛЮЧЕВЫЕ ОСОБЕННОСТИ ПРОЦЕССА

### 1. Staging таблица
Промежуточное хранилище позволяет:
- Повторно парсить HTML без повторной загрузки
- Тестировать разные версии промптов на одних данных
- Отслеживать историю изменений

### 2. Версионирование промптов
Каждая версия промпта сохраняется как `llm_prompt_version`, что позволяет:
- Сравнивать эффективность разных версий
- Откатываться к предыдущим версиям
- Анализировать историю улучшений

### 3. Обработка ошибок
При ошибках парсинга:
- Ошибки сохраняются в `parsing_errors`
- Записи помечаются `needs_reparse = TRUE` для повторной обработки
- Можно перезапустить процесс с того же места

### 4. Батчевая обработка
- Записи обрабатываются батчами (по умолчанию 25)
- Можно продолжить с места остановки при сбоях
- Прогресс отслеживается через SQL запросы

### 5. Мониторинг
SQL запросы для отслеживания:
- Прогресса обработки
- Качества данных по версиям промптов
- Проблемных записей

---

## 💰 ПРЕИМУЩЕСТВА STAGING-ПОДХОДА

### Экономия средств:
При 5 итерациях улучшения промпта:
- **Прямой путь:** $76.62 (каждый раз заново качать HTML)
- **Staging путь:** $23.10 (HTML качается один раз)
- **Экономия:** $53.52 (70% дешевле!)

### Гибкость:
- Можно менять промпты и переобрабатывать данные без повторной загрузки HTML
- Легко тестировать разные версии промптов на одних данных
- Возможность A/B тестирования подходов

### Надежность:
- Возможность повторной обработки при ошибках
- Сохранение истории всех попыток парсинга
- Отслеживание качества по версиям

### История:
- Отслеживание эффективности разных версий промптов
- Анализ улучшений качества данных
- Возможность отката к предыдущим версиям

---

## 📊 СТРУКТУРА ДАННЫХ

### Staging таблица (`autumna_staging`):
- `html_content` - исходный HTML (сохраняется один раз)
- `parsed_json` - результат парсинга OpenAI (обновляется многократно)
- `llm_prompt_version` - версия промпта для отслеживания
- `data_quality_score` - оценка качества данных (0-100+)
- `extraction_confidence` - уверенность в извлечении
- `needs_reparse` - флаг для повторной обработки
- `processed` - флаг обработки в финальную БД

### Финальная БД (`care_homes`):
- 93 поля (76 плоских + 17 JSONB)
- Валидированные и нормализованные данные
- Готовые для использования в приложении

---

## 🎯 РЕЗЮМЕ

Процесс парсинга организован по **staging-подходу** с тремя фазами:

1. **Фаза 1:** Загрузка HTML через Firecrawl ($15 один раз)
2. **Фаза 2:** Парсинг через OpenAI LLM ($1.62 многократно)
3. **Фаза 3:** Маппинг в финальную БД ($0)

Этот подход позволяет **итеративно улучшать качество парсинга** без повторной загрузки HTML, что критично на этапе разработки и тестирования.

**Ключевые преимущества:**
- ✅ Экономия 70% при итерациях
- ✅ Гибкость в тестировании промптов
- ✅ Надежность и возможность повторной обработки
- ✅ История и отслеживание эффективности

---

**Дата создания:** 3 ноября 2025  
**Версия:** v2.4  
**Статус:** ✅ Production Ready

