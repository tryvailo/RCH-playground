# 🔄 МИГРАЦИЯ НА MARKDOWN ФОРМАТ

**Дата:** 3 ноября 2025  
**Версия:** v2.4 → v2.5 (Markdown support)  
**Статус:** ✅ Готово к применению

---

## 📋 ЧТО ИЗМЕНИЛОСЬ

Система парсинга переведена с HTML на **Markdown формат** для:
- 💰 Экономии 30-50% токенов при парсинге
- 🎯 Улучшения качества извлечения данных
- ⚡ Ускорения обработки

---

## 🔧 ШАГИ МИГРАЦИИ

### Шаг 1: Обновить SQL схему

Выполните SQL скрипт для добавления поля `markdown_content`:

```bash
psql -U postgres -d care_homes -f input/autumna/staging/01_add_markdown_column.sql
```

Это добавит:
- Поле `markdown_content TEXT` в таблицу `autumna_staging`
- Обновленные представления (views) для поддержки markdown
- Индексы для быстрого поиска

**Важно:** Старое поле `html_content` остается для обратной совместимости.

---

### Шаг 2: Обновить код (уже сделано ✅)

Изменения внесены в:
- ✅ `phase1_load_html.py` - запрашивает markdown формат
- ✅ `phase2_parse_llm.py` - использует markdown_content для парсинга
- ✅ `AUTUMNA_PARSING_PROMPT_v2_4.md` - обновлен для работы с markdown

---

### Шаг 3: Перезагрузить данные (опционально)

Если у вас уже есть данные в `html_content`, у вас есть два варианта:

#### Вариант A: Использовать существующие HTML (временно)

Система будет работать с `html_content` если `markdown_content` пустой. Но рекомендуется перезагрузить.

#### Вариант B: Перезагрузить через Firecrawl (рекомендуется)

```bash
# Пометить все записи для перезагрузки
psql -d care_homes -c "
UPDATE autumna_staging
SET needs_reparse = TRUE
WHERE markdown_content IS NULL;
"

# Запустить phase1_load_html.py для загрузки markdown
python input/autumna/staging/phase1_load_html.py \
    --urls urls.txt \
    --api-key $FIRECRAWL_API_KEY \
    --api-version v2.5 \
    --use-cache
```

---

## 🚀 ИСПОЛЬЗОВАНИЕ

### Загрузка Markdown (Фаза 1)

```bash
python input/autumna/staging/phase1_load_html.py \
    --urls urls.txt \
    --api-key $FIRECRAWL_API_KEY \
    --api-version v2.5 \
    --use-cache
```

**Что происходит:**
- Firecrawl запрашивает markdown формат (`format: "markdown"`)
- Markdown сохраняется в `markdown_content`
- Старое поле `html_content` остается пустым (или заполнено для обратной совместимости)

### Парсинг Markdown (Фаза 2)

```bash
python input/autumna/staging/phase2_parse_llm.py \
    --prompt-version v2.4 \
    --batch-size 25
```

**Что происходит:**
- Скрипт читает `markdown_content` из БД
- Отправляет markdown в OpenAI API
- Промпт оптимизирован для работы с markdown форматом

---

## 📊 ПРЕИМУЩЕСТВА

### Экономия токенов

| Формат | Средний размер | Токенов (примерно) | Стоимость за 300 страниц |
|--------|---------------|-------------------|------------------------|
| HTML   | 200-500 KB    | 50,000-125,000    | $1.50-3.75             |
| **Markdown** | **50-150 KB** | **12,500-37,500** | **$0.38-1.12**         |

**Экономия:** ~70% стоимости парсинга!

### Улучшение качества

- ✅ Markdown лучше структурирован для LLM
- ✅ Меньше визуального шума
- ✅ Фокус на контенте, а не на разметке
- ✅ Сохранение важной структуры (заголовки, списки, таблицы)

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Обратная совместимость

- Старое поле `html_content` остается в БД
- Старые записи продолжат работать
- Новые записи будут использовать `markdown_content`

### Миграция существующих данных

Если нужно перезагрузить существующие данные:

```sql
-- Пометить записи для перезагрузки
UPDATE autumna_staging
SET needs_reparse = TRUE
WHERE markdown_content IS NULL
  AND html_content IS NOT NULL;
```

Затем запустить `phase1_load_html.py` снова.

---

## 🔍 ПРОВЕРКА

### Проверить статистику

```sql
SELECT * FROM v_staging_stats;
```

Должно показать:
- `markdown_loaded` - количество записей с markdown
- `html_loaded` - количество записей с HTML (старые)

### Проверить проблемные записи

```sql
SELECT * FROM v_staging_problems LIMIT 10;
```

---

## 📝 ИЗМЕНЕННЫЕ ФАЙЛЫ

1. ✅ `input/autumna/staging/phase1_load_html.py`
   - Запрашивает `format: "markdown"` вместо `"html"`
   - Сохраняет в `markdown_content`

2. ✅ `input/autumna/staging/phase2_parse_llm.py`
   - Читает `markdown_content` вместо `html_content`
   - Функция `parse_markdown_with_openai()` вместо `parse_html_with_openai()`

3. ✅ `input/autumna/AUTUMNA_PARSING_PROMPT_v2_4.md`
   - Обновлен для работы с markdown форматом
   - Изменены инструкции по извлечению данных

4. ✅ `input/autumna/staging/01_add_markdown_column.sql`
   - SQL миграция для добавления поля `markdown_content`

---

## ✅ ГОТОВО!

После выполнения миграции:
- ✅ Новые данные будут загружаться в markdown формате
- ✅ Парсинг будет использовать markdown (экономия 70%)
- ✅ Старые данные продолжат работать
- ✅ Можно постепенно мигрировать существующие данные

---

**Дата создания:** 3 ноября 2025  
**Версия:** v2.5 (Markdown support)  
**Статус:** ✅ Готово к использованию

