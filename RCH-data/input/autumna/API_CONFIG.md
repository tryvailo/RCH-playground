# 🔐 Конфигурация API ключей

## Файл `.env`

API ключ OpenAI сохранен в файле `.env` в корне проекта.

### Структура файла:

```bash
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-2024-08-06
```

### Безопасность:

✅ Файл `.env` уже добавлен в `.gitignore` - он **НЕ будет** закоммичен в git  
✅ Права доступа установлены на `600` (только владелец может читать/писать)

### Использование:

#### Вариант 1: Автоматическая загрузка (рекомендуется)

Установите `python-dotenv`:
```bash
pip install python-dotenv
```

Скрипты автоматически загрузят переменные из `.env` файла.

#### Вариант 2: Ручная загрузка

Экспортируйте переменные вручную:
```bash
export OPENAI_API_KEY=sk-proj-...
export OPENAI_MODEL=gpt-4o-2024-08-06
```

Или загрузите из файла:
```bash
source .env  # или
set -a; source .env; set +a
```

### Проверка:

Проверить, что ключ загружен:
```bash
python3 -c "import os; print('✅ API Key:', os.getenv('OPENAI_API_KEY', 'NOT FOUND')[:20] + '...')"
```

### Важно:

⚠️ **НИКОГДА не коммитьте `.env` файл в git!**  
⚠️ **НИКОГДА не публикуйте API ключ публично!**  
⚠️ Если ключ скомпрометирован - немедленно отзовите его в OpenAI Dashboard

---

**Дата создания:** 9 ноября 2025  
**Статус:** ✅ API ключ сохранен в `.env` файл

